cmake_minimum_required(VERSION 3.16)
project(microlind-driver)

# This directory contains driver source files that are included by other projects
# No build targets needed here as the files are used as dependencies

message(STATUS "Driver directory - source files only")
