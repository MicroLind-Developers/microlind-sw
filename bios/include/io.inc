; MicroLind BIOS and System Memory Map,
; Memory area: $C000 - $DFFF (8kB in Bank 4)

IO_INC EQU 1

; -----------------------------------------------------------------
; IO Base Address
; $F400 - $F7FF
; -----------------------------------------------------------------

IO_BASE EQU $F400 EXPORT

; -----------------------------------------------------------------
; Memory Mapper (MMU) 
; $F400 - $F403
; -----------------------------------------------------------------
MMU_BASE EQU IO_BASE+0 EXPORT

; -----------------------------------------------------------------
; IRQ Handler
; $F404
; -----------------------------------------------------------------
IRQ_BASE EQU IO_BASE+4 EXPORT

; -----------------------------------------------------------------
; Spare 1 
; $F405 - $F40F
; -----------------------------------------------------------------
SPARE_1_BASE EQU IO_BASE+5 EXPORT

; -----------------------------------------------------------------
; PS/2
; $F410 - $F411
; -----------------------------------------------------------------
PS2_BASE EQU IO_BASE+16 EXPORT

; -----------------------------------------------------------------
; RTC
; $F412 - $F417
; -----------------------------------------------------------------
RTC_BASE EQU IO_BASE+18 EXPORT

; -----------------------------------------------------------------
; Compact Flash
; $F418 - $F41F
; -----------------------------------------------------------------
CF_BASE EQU IO_BASE+24 EXPORT

; -----------------------------------------------------------------
; Parallel (VIA)
; $F420 - $F42F
; -----------------------------------------------------------------
PARALLEL_BASE EQU IO_BASE+32 EXPORT

; -----------------------------------------------------------------
; Serial (XR88C92)
; $F430 - $F43F
; -----------------------------------------------------------------
SERIAL_BASE EQU IO_BASE+48 EXPORT

; -----------------------------------------------------------------
; Graphics
; $F440 - $F47F
; -----------------------------------------------------------------
GRAPHICS_BASE EQU IO_BASE+64 EXPORT

; -----------------------------------------------------------------
; Audio
; $F480 - $F4BF
; -----------------------------------------------------------------
AUDIO_BASE EQU IO_BASE+128 EXPORT

; -----------------------------------------------------------------
; Spare 2
; $F4C0 - $F4FF
; -----------------------------------------------------------------
SPARE_2_BASE EQU IO_BASE+192 EXPORT

; -----------------------------------------------------------------
; Expansion Port
; $F500 - $F7FF
; -----------------------------------------------------------------
EXPANSION_PORT_BASE EQU IO_BASE+256 EXPORT

