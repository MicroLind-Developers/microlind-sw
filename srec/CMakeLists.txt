cmake_minimum_required(VERSION 3.16)
project(microlind-srec)

# This directory contains srec source files
# No build targets needed here as the files are used as dependencies

message(STATUS "SREC directory - source files only")
